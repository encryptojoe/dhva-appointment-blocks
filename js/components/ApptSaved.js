import React from 'react';
import { Row, Col, Alert} from 'react-bootstrap';

import React from 'react';

export const ApptSaved = React.createClass({
    render() {
        return (
            <div></div>
        );
    }
});
